<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../../../../../../vendor/magento/magento2-functional-testing-framework/src/Magento/FunctionalTestingFramework/Test/etc/testSchema.xsd">
    <test name="AdminManageSourcesSearchByKeywordTest">
        <annotations>
            <features value="Multi-Store Inventory"/>
            <stories value="Search By Keyword in Grid"/>
            <title value="Search by Keyword on the Manage Sources grid"/>
            <description value="You should be able to Search by Keyword from the Manage Sources grid."/>
            <testCaseId value="506"/>
            <group value="msi"/>
        </annotations>
        <before>
            <createData entity="_defaultSource" stepKey="createSource1"/>
            <createData entity="_defaultSource" stepKey="createSource2"/>
            <createData entity="_defaultSource" stepKey="createSource3"/>
            <createData entity="_defaultSource" stepKey="createSource4"/>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logoutOfAdmin"/>
        </after>

        <amOnPage url="{{ManageSources.url}}" stepKey="amOnTheSourcesListPage"/>
        <waitForPageLoad stepKey="waitForPageLoad1"/>

        <actionGroup ref="AdminClearGridFiltersIfPresent" stepKey="clearGridFilterIfPresent"/>
        <fillField selector="{{AdminGridSearchBox.searchByKeyword}}" userInput="$$createSource3.general[name]$$" stepKey="fillSearchByKeyword"/>
        <click selector="{{AdminGridSearchBox.search}}" stepKey="clickOnSearch"/>

        <see userInput="$$createSource3.general[source_code]$$" selector="{{AdminGridRow.rowOne}}" stepKey="seeCodeInRow1"/>
        <see userInput="$$createSource3.general[name]$$" selector="{{AdminGridRow.rowOne}}" stepKey="seeNameInRow1"/>
        <see userInput="Enabled" selector="{{AdminGridRow.rowOne}}" stepKey="seeIsEnabledInRow1"/>
        <!--Todo: MQE-677 needs to be merged into Develop before we can use "0" in the Data. Hardcoded "0" for now.-->
        <see userInput="0" selector="{{AdminGridRow.rowOne}}" stepKey="seePriorityInRow1"/>
    </test>
</tests>
